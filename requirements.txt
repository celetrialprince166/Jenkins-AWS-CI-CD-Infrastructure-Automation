# ============================================================================
# Python Dependencies for Jenkins Infrastructure
# ============================================================================
# This file enables pip caching in GitHub Actions for faster CI/CD runs.
# 
# USAGE:
#   pip install -r requirements.txt
#
# NOTE: We use ansible-core (not ansible) because:
#   - ansible-core uses versions like 2.16.x (the actual engine)
#   - ansible uses versions like 9.x, 10.x (the full collection bundle)
# ============================================================================

# Ansible Core - Configuration Management Engine
ansible-core==2.16.0

# Ansible Linting Tools
ansible-lint>=6.22.0
yamllint>=1.33.0

# AWS SDK for Python (used by some Ansible modules)
boto3>=1.34.0
botocore>=1.34.0
